# 🔐 دليل ملفات البيئة (.env) - مشروع وصلني

## 📋 ملفات البيئة المتوفرة

| الملف | الاستخدام | الحالة |
|-------|-----------|--------|
| `.env` | بيئة التطوير | ✅ موجود وجاهز |
| `.env.testing` | بيئة الاختبارات | ✅ موجود |
| `.env.production.example` | مثال للإنتاج | ✅ موجود (نسخه للإنتاج) |

---

## 🎯 ملف .env الحالي

### ✅ الإعدادات الجاهزة

```env
# معلومات التطبيق
APP_NAME="وصلني"
APP_ENV=local
APP_KEY=                              # سيتم توليده تلقائياً
APP_DEBUG=true
APP_URL=http://localhost:8000
APP_LOCALE=ar                         # اللغة العربية
APP_TIMEZONE=Asia/Riyadh              # توقيت السعودية

# قاعدة البيانات
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=wasalni
DB_USERNAME=root
DB_PASSWORD=                          # ضع كلمة مرورك هنا

# التخزين والجلسات
SESSION_DRIVER=database
CACHE_STORE=database
QUEUE_CONNECTION=database

# إعدادات النظام
SYSTEM_COMMISSION_PERCENTAGE=10       # عمولة النظام
BASE_PRICE_PER_KM=5                   # السعر لكل كم
```

---

## 🚀 خطوات الإعداد

### 1️⃣ ملف .env موجود بالفعل
لا تحتاج إلى نسخ `.env.example` - الملف جاهز! ✅

### 2️⃣ فقط عدّل إعدادات قاعدة البيانات
افتح ملف `.env` وعدّل:
```env
DB_DATABASE=wasalni
DB_USERNAME=root
DB_PASSWORD=your_password_here    # ضع كلمة مرورك
```

### 3️⃣ ولّد مفتاح التطبيق
```bash
php artisan key:generate
```

✅ **هذا كل شيء! الملف جاهز للاستخدام.**

---

## 📝 شرح الإعدادات المهمة

### 🔹 إعدادات التطبيق

```env
APP_NAME="وصلني"              # اسم التطبيق
APP_ENV=local                  # البيئة: local/production/testing
APP_DEBUG=true                 # عرض الأخطاء (false في الإنتاج)
APP_URL=http://localhost:8000  # رابط التطبيق
```

### 🔹 إعدادات قاعدة البيانات

```env
DB_CONNECTION=mysql            # نوع القاعدة
DB_HOST=127.0.0.1             # عنوان السيرفر
DB_PORT=3306                  # المنفذ
DB_DATABASE=wasalni           # اسم القاعدة
DB_USERNAME=root              # اسم المستخدم
DB_PASSWORD=                  # كلمة المرور
```

### 🔹 إعدادات النظام الخاصة

```env
SYSTEM_COMMISSION_PERCENTAGE=10  # نسبة العمولة (10%)
BASE_PRICE_PER_KM=5             # السعر الأساسي لكل كيلومتر
```

**يمكنك تغيير هذه القيم حسب احتياجك:**
- العمولة: 10% (يمكن تغييرها إلى 15% مثلاً)
- السعر: 5 ريال/كم (يمكن تغييره إلى 7 ريال/كم)

---

## 🔮 المرحلة الثانية (خدمات خارجية)

### هذه الإعدادات جاهزة لكن غير مفعّلة:

```env
# Firebase للإشعارات (المرحلة 2)
FIREBASE_SERVER_KEY=
FIREBASE_API_KEY=

# Google Maps (المرحلة 2)
GOOGLE_MAPS_API_KEY=

# Stripe للدفع (المرحلة 2)
STRIPE_KEY=
STRIPE_SECRET=

# Twilio للـ SMS (المرحلة 2)
TWILIO_SID=
TWILIO_TOKEN=
TWILIO_FROM=
```

**متى تحتاجها؟**
- عندما تريد إرسال إشعارات فورية → Firebase
- عندما تريد استخدام الخرائط → Google Maps
- عندما تريد الدفع الإلكتروني → Stripe
- عندما تريد إرسال OTP عبر SMS → Twilio

---

## 🧪 ملف .env.testing

للاختبارات فقط:

```env
APP_NAME="وصلني - Testing"
APP_ENV=testing
DB_DATABASE=wasalni_testing    # قاعدة بيانات منفصلة للاختبار
CACHE_DRIVER=array
SESSION_DRIVER=array
MAIL_MAILER=array
```

**لا تحتاج تعديله الآن** - جاهز للاستخدام! ✅

---

## 🏭 ملف .env.production.example

**للإنتاج فقط!** عندما تنشر المشروع على سيرفر حقيقي:

### خطوات الإنتاج:
```bash
# 1. انسخ الملف
cp .env.production.example .env

# 2. عدّل الإعدادات
# - غير APP_DEBUG إلى false
# - ضع رابط الموقع الحقيقي
# - استخدم Redis للـ Cache
# - فعّل الخدمات الخارجية

# 3. ولّد مفتاح جديد
php artisan key:generate
```

### إعدادات مهمة للإنتاج:
```env
APP_DEBUG=false                      # أوقف عرض الأخطاء
APP_URL=https://yourdomain.com       # الرابط الحقيقي
LOG_LEVEL=error                      # سجّل الأخطاء فقط
CACHE_STORE=redis                    # استخدم Redis
SESSION_DRIVER=redis                 # جلسات في Redis
QUEUE_CONNECTION=redis               # Queues في Redis
```

---

## ✅ قائمة التحقق

قبل التشغيل، تأكد من:

- [x] ملف `.env` موجود ✅
- [ ] عدّلت `DB_PASSWORD`
- [ ] أنشأت قاعدة البيانات `wasalni`
- [ ] ولّدت مفتاح التطبيق (`php artisan key:generate`)
- [ ] شغّلت Migrations (`php artisan migrate`)
- [ ] شغّلت Seeders (`php artisan db:seed`)

---

## 🔍 كيف أعرف أن الإعدادات صحيحة؟

### اختبار قاعدة البيانات:
```bash
php artisan migrate
```

إذا نجح → إعدادات قاعدة البيانات صحيحة ✅

### اختبار التطبيق:
```bash
php artisan serve
```

افتح http://localhost:8000 - إذا ظهرت الصفحة → كل شيء جاهز! ✅

---

## 🆘 مشاكل شائعة

### ❌ خطأ: "Connection refused"
**السبب:** قاعدة البيانات لم تُنشأ أو بيانات الاتصال خاطئة

**الحل:**
```bash
mysql -u root -p
CREATE DATABASE wasalni;
exit;
```

ثم تأكد من `DB_PASSWORD` في `.env`

---

### ❌ خطأ: "No application encryption key"
**السبب:** لم تُولّد المفتاح

**الحل:**
```bash
php artisan key:generate
```

---

### ❌ خطأ: "Class not found"
**السبب:** لم تُثبّت التبعيات

**الحل:**
```bash
composer install
```

---

## 📊 جدول مقارنة البيئات

| الإعداد | Development (.env) | Testing (.env.testing) | Production |
|---------|-------------------|----------------------|------------|
| APP_DEBUG | true | true | **false** |
| LOG_LEVEL | debug | debug | **error** |
| CACHE | database | array | **redis** |
| SESSION | database | array | **redis** |
| QUEUE | database | sync | **redis** |
| DB_NAME | wasalni | wasalni_testing | wasalni_prod |

---

## 🎓 نصائح مهمة

### ✅ أفضل الممارسات:

1. **لا تشارك ملف .env أبداً!**
   - `.env` في `.gitignore` بالفعل ✅

2. **استخدم قيم مختلفة لكل بيئة**
   - Development: DB=wasalni
   - Testing: DB=wasalni_testing
   - Production: DB=wasalni_prod

3. **للإنتاج:**
   - APP_DEBUG=false
   - استخدم Redis
   - استخدم HTTPS
   - قيم أمنية قوية

4. **احتفظ بنسخة احتياطية من .env**
   - لكن **ليس على Git!**

---

## 📞 هل تحتاج مساعدة؟

**إذا واجهتك مشكلة:**

1. راجع `INSTALLATION.md` للتفاصيل
2. تحقق من `storage/logs/laravel.log`
3. تأكد من أن MySQL يعمل

---

<div align="center">

**ملف .env جاهز وموجود! ✅**

**كل ما تحتاجه:**
1. عدّل `DB_PASSWORD`
2. شغّل `php artisan key:generate`
3. شغّل `php artisan migrate --seed`

**ثم استمتع بالتطوير! 🚀**

</div>

