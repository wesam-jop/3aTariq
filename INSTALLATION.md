# 🚀 دليل التثبيت والتشغيل - مشروع وصلني

## المتطلبات الأساسية

قبل البدء، تأكد من توفر المتطلبات التالية على جهازك:

- PHP 8.2 أو أحدث
- Composer
- Node.js 18+ و NPM
- MySQL 8.0+
- Git

## خطوات التثبيت

### 1. تثبيت التبعيات

```bash
# تثبيت تبعيات PHP
composer install

# تثبيت تبعيات JavaScript
npm install
```

### 2. إعداد ملف البيئة

ملف `.env` موجود بالفعل في المشروع. تأكد من تعديل إعدادات قاعدة البيانات:

```env
DB_DATABASE=wasalni
DB_USERNAME=root
DB_PASSWORD=your_password
```

### 3. إنشاء قاعدة البيانات

قم بإنشاء قاعدة بيانات MySQL جديدة:

```sql
CREATE DATABASE wasalni CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 4. توليد مفتاح التطبيق

```bash
php artisan key:generate
```

### 5. تشغيل Migrations و Seeders

```bash
# تشغيل Migrations لإنشاء الجداول
php artisan migrate

# تشغيل Seeders لإضافة البيانات التجريبية
php artisan db:seed
```

### 6. ربط مجلد التخزين

```bash
php artisan storage:link
```

### 7. تشغيل المشروع

افتح نافذتي terminal:

**Terminal 1 - Laravel Server:**
```bash
php artisan serve
```

**Terminal 2 - Vite Dev Server:**
```bash
npm run dev
```

## 🔐 بيانات الدخول التجريبية

بعد تشغيل Seeders، يمكنك استخدام البيانات التالية:

### لوحة الإدارة (Admin)
- الرابط: http://localhost:8000/admin
- الهاتف: `0500000000`
- كلمة المرور: `password`

### حساب سائق
- الهاتف: `0501111111`
- كلمة المرور: `password`

### حساب عميل
- الهاتف: `0503333333`
- كلمة المرور: `password`

## 📦 البناء للإنتاج

```bash
# بناء ملفات JavaScript
npm run build

# تحسين الأداء
php artisan config:cache
php artisan route:cache
php artisan view:cache
```

## 🧪 تشغيل الاختبارات

```bash
php artisan test
```

## 🔧 حل المشاكل الشائعة

### مشكلة: الصور لا تظهر
```bash
php artisan storage:link
```

### مشكلة: أخطاء في الصلاحيات
```bash
chmod -R 775 storage bootstrap/cache
```

### مشكلة: Vite لا يعمل
```bash
npm cache clean --force
rm -rf node_modules package-lock.json
npm install
```

## 📝 ملاحظات مهمة

1. **OTP في البيئة التطويرية:** كود OTP يظهر في response للتطوير فقط. في الإنتاج يجب دمج Twilio أو Firebase.

2. **المدفوعات:** نظام الدفع الإلكتروني سيتم إضافته في المرحلة الثانية.

3. **الإشعارات:** نظام الإشعارات الفورية سيتم إضافته في المرحلة الثانية.

4. **الخرائط:** التكامل مع Google Maps سيتم في المرحلة الثانية.

## 🌐 الروابط المهمة

- **الصفحة الرئيسية:** http://localhost:8000
- **لوحة الإدارة:** http://localhost:8000/admin
- **لوحة العميل:** http://localhost:8000/customer/dashboard
- **لوحة السائق:** http://localhost:8000/driver/dashboard
- **API Documentation:** قريباً

## 📞 الدعم

في حالة وجود مشاكل، يرجى مراجعة:
- ملف README.md للمزيد من المعلومات
- سجلات Laravel في `storage/logs/laravel.log`

---

**ملاحظة:** هذا المشروع في مرحلة MVP (Minimum Viable Product) - النسخة الأولية الأساسية.

