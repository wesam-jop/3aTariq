# تطبيق التوصيل - Delivery App

تطبيق شامل لإدارة طلبات التوصيل والرحلات مع ثلاثة أنواع من المستخدمين: الإدارة، العملاء، والسائقين.

## المميزات

### 🔐 نظام المصادقة
- تسجيل الدخول والخروج
- إنشاء حسابات جديدة للعملاء والسائقين
- نظام OTP للتحقق من الهاتف
- حماية الصفحات حسب نوع المستخدم

### 👨‍💼 لوحة تحكم الإدارة
- إحصائيات شاملة (المستخدمين، الطلبات، الرحلات، الإيرادات)
- إدارة المستخدمين
- إدارة الطلبات
- عرض التقارير

### 👤 لوحة تحكم العميل
- إنشاء طلبات توصيل جديدة
- عرض الرحلات المتاحة
- تتبع طلباتي
- حجز الرحلات

### 🚗 لوحة تحكم السائق
- إنشاء رحلات جديدة
- عرض الطلبات المخصصة
- إدارة رحلاتي
- تحديث حالة الطلبات

## التقنيات المستخدمة

### Backend
- **Laravel 12** - إطار عمل PHP
- **Laravel Sanctum** - للمصادقة API
- **Laravel Inertia.js** - للربط بين Laravel و React
- **SQLite** - قاعدة البيانات

### Frontend
- **React 18** - مكتبة JavaScript
- **Inertia.js** - للربط مع Laravel
- **Tailwind CSS** - للتصميم
- **Vite** - أداة البناء

## التثبيت والتشغيل

### المتطلبات
- PHP 8.2+
- Composer
- Node.js 18+
- npm أو yarn

### خطوات التثبيت

1. **استنساخ المشروع**
```bash
git clone <repository-url>
cd delivery-api
```

2. **تثبيت dependencies**
```bash
composer install
npm install
```

3. **إعداد البيئة**
```bash
cp .env.example .env
php artisan key:generate
```

4. **إعداد قاعدة البيانات**
```bash
php artisan migrate
```

5. **بناء الأصول**
```bash
npm run build
```

6. **تشغيل الخادم**
```bash
php artisan serve
```

### للتطوير
```bash
# تشغيل خادم Laravel
php artisan serve

# تشغيل Vite للتطوير
npm run dev
```

## هيكل المشروع

```
delivery-api/
├── app/
│   ├── Http/
│   │   ├── Controllers/
│   │   │   ├── Auth/          # تحكم المصادقة
│   │   │   ├── Admin/         # تحكم الإدارة
│   │   │   ├── Customer/      # تحكم العملاء
│   │   │   ├── Driver/        # تحكم السائقين
│   │   │   └── Api/           # API للجوال
│   │   └── Middleware/        # حماية الصفحات
│   └── Models/               # نماذج البيانات
├── resources/
│   └── js/
│       └── Pages/            # صفحات React
│           ├── Auth/         # صفحات المصادقة
│           ├── Admin/        # صفحات الإدارة
│           ├── Customer/     # صفحات العملاء
│           ├── Driver/       # صفحات السائقين
│           └── Layouts/      # تخطيطات الصفحات
├── routes/
│   ├── web.php              # مسارات الويب
│   └── api.php              # مسارات API
└── database/
    └── migrations/          # هجرة قاعدة البيانات
```

## API للجوال

المشروع يحتوي على API كاملة للاستخدام مع تطبيقات الجوال:

### المصادقة
- `POST /api/send-otp` - إرسال OTP
- `POST /api/verify-otp` - التحقق من OTP
- `POST /api/login` - تسجيل الدخول
- `POST /api/logout` - تسجيل الخروج

### الطلبات
- `GET /api/orders` - عرض الطلبات
- `POST /api/orders` - إنشاء طلب جديد
- `PUT /api/orders/{id}` - تحديث الطلب
- `POST /api/orders/{id}/accept` - قبول الطلب
- `POST /api/orders/{id}/complete` - إكمال الطلب

### الرحلات
- `GET /api/trips` - عرض الرحلات
- `POST /api/trips` - إنشاء رحلة جديدة
- `POST /api/trips/{id}/book` - حجز الرحلة

### الدردشة
- `GET /api/chats` - عرض المحادثات
- `GET /api/chats/{id}/messages` - عرض الرسائل
- `POST /api/chats/{id}/messages` - إرسال رسالة

## أنواع المستخدمين

### 1. الإدارة (Admin)
- إدارة النظام بالكامل
- عرض الإحصائيات
- إدارة المستخدمين والطلبات

### 2. العميل (Customer)
- إنشاء طلبات التوصيل
- حجز الرحلات
- تتبع الطلبات

### 3. السائق (Driver)
- إنشاء الرحلات
- قبول الطلبات
- تحديث حالة التسليم

## الأمان

- حماية CSRF
- تشفير كلمات المرور
- التحقق من الصلاحيات
- حماية API بـ Sanctum

## التطوير المستقبلي

- [ ] إشعارات فورية
- [ ] تتبع الموقع المباشر
- [ ] نظام التقييمات
- [ ] دفع إلكتروني
- [ ] تقارير متقدمة

## المساهمة

نرحب بالمساهمات! يرجى إنشاء Pull Request أو فتح Issue للمناقشة.

## الترخيص

هذا المشروع مرخص تحت رخصة MIT.